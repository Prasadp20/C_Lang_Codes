
	// WAP to print all the composite numbers between a given range 
	
	#include<stdio.h>
	void main()
	{
		int fromNumber, toNumber;
		printf("Enter the range to print composite numbers between them : \n");
		printf("From: ");
		scanf("%d",&fromNumber);

		printf("To: ");
		scanf("%d",&toNumber);

		for(int i = fromNumber; i <= toNumber; i++)
		{
			if(i % 
		}
	}
